query getSettingsData($password: String!) {
    getGlobalSettings(password: $password) {
        failureRetries
        pollingInterval
        pollingTimeout
        ppmWindow
        referenceServer
        retentionDays
    }
}
